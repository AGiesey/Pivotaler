import { createMuiTheme } from '@material-ui/core/styles';
import blue from '@material-ui/core/colors/blue';
import orange from '@material-ui/core/colors/orange';

export const pivotalerTheme = createMuiTheme({
  palette: {
    primary: blue,
    secondary: orange,
  },
})